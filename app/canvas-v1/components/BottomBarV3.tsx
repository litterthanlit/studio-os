"use client";

/**
 * V3 Bottom Bar — previously showed zoom controls.
 * Zoom UI now lives in the inspector header (top-right).
 * Zoom keyboard shortcuts (Cmd+= / Cmd+-) and scroll-to-zoom are in useCanvasGestures.ts.
 * Component kept for rollback — renders nothing.
 */

export function BottomBarV3() {
  return null;
}
