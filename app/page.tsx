import { CursorSite } from "@/components/marketing-cursor/cursor-site";

export const metadata = {
  title: "Studio OS — Cursor",
  description:
    "References to taste to canvas to code — a single Studio OS story.",
};

export default function HomePage() {
  return <CursorSite />;
}
